import { describe, it } from 'vitest';

// FRONTEND-ONLY MODE: Supabase RLS tests disabled
describe.skip('RLS Policy Tests (FRONTEND-ONLY MODE - SKIPPED)', () => {
  it('should skip all Supabase tests in frontend-only mode', () => {
    // Tests skipped
  });
});
