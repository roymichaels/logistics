import { describe, it } from 'vitest';

// FRONTEND-ONLY MODE: Supabase session tests disabled
describe.skip('Session Persistence Tests (FRONTEND-ONLY MODE - SKIPPED)', () => {
  it('should skip all Supabase session tests in frontend-only mode', () => {
    // Tests skipped
  });
});
