import { describe, it } from 'vitest';

// FRONTEND-ONLY MODE: Supabase permission tests disabled
describe.skip('Permission System Tests (FRONTEND-ONLY MODE - SKIPPED)', () => {
  it('should skip all Supabase permission tests in frontend-only mode', () => {
    // Tests skipped
  });
});
