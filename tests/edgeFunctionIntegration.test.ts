import { describe, it } from 'vitest';

// FRONTEND-ONLY MODE: Edge function tests disabled
describe.skip('Edge Function Integration Tests (FRONTEND-ONLY MODE - SKIPPED)', () => {
  it('should skip all edge function tests in frontend-only mode', () => {
    // Tests skipped
  });
});
