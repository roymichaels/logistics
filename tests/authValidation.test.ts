import { describe, it } from 'vitest';

// FRONTEND-ONLY MODE: Supabase auth tests disabled
describe.skip('Auth Validation Tests (FRONTEND-ONLY MODE - SKIPPED)', () => {
  it('should skip all Supabase auth tests in frontend-only mode', () => {
    // Tests skipped
  });
});
